<%= form_with(model: @travel_cost, local: true) do |f| %>

<%= f.label :name, '項目名' %>
<%= f.text_field :name %>

<%= f.label :budget, '予算' %>
<%= f.text_field :budget %>

<%= f.label :amount_paid, '支払った金額' %>
<%= f.text_field :amount_paid %>

<%= f.submit '保存' %>

<% end %>